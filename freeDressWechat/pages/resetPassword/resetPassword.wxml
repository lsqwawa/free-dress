<!-- 重置密码页 · NEW PASSWORD -->
<view class="reset-page grain-bg">
  <view class="nav-bar">
    <view class="nav-back" bindtap="goBack">
      <text class="nav-arrow">←</text>
      <text class="text-kicker back-label">BACK</text>
    </view>
  </view>

  <scroll-view scroll-y class="scroll-wrap">
    <view class="title-block anim-fade-in-up">
      <text class="text-kicker title-kicker">NEW PASSWORD</text>
      <text class="text-serif-title title-main">设置新密码</text>
      <view class="title-divider"></view>
      <text class="text-caption tagline">请输入 6-20 位新密码</text>
    </view>

    <view class="form anim-fade-in-up delay-1">
      <view class="input-group">
        <text class="input-label text-caption">新密码</text>
        <input class="input-field" password="{{true}}" maxlength="20" placeholder="6-20 位" placeholder-class="input-placeholder" value="{{newPassword}}" bindinput="onInput" data-field="newPassword" />
        <view class="input-line"></view>
      </view>

      <view class="input-group">
        <text class="input-label text-caption">确认新密码</text>
        <input class="input-field" password="{{true}}" maxlength="20" placeholder="请再次输入新密码" placeholder-class="input-placeholder" value="{{confirmPassword}}" bindinput="onInput" data-field="confirmPassword" />
        <view class="input-line"></view>
      </view>
    </view>

    <view class="submit-wrap anim-fade-in-up delay-2">
      <button class="btn-primary" bindtap="handleReset" loading="{{loading}}">
        <text class="btn-text">确认重置</text>
        <text class="btn-arrow">✓</text>
      </button>
    </view>
  </scroll-view>
</view>
